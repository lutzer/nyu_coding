export const title = "Arrays";

# Working with Arrays

Here's how `.map()` works:

<SandpackSnippet folder="arrays/basic" template="react" fullscreen="true" />

Now try `.filter()` yourself:

<SandpackSnippet folder="arrays/filter" template="react" />
