export const title = "Intro";

# Welcome

This is an `.mdx` page. Write markdown normally, and drop a `<SandpackSnippet>`
anywhere you want an editor. The code comes from `src/snippets/<folder>/`.

<SandpackSnippet folder="intro/hello" template="react" />

You can change the `template` per snippet:

<SandpackSnippet folder="intro/vanilla" template="vanilla" activeFile="/index.js" />
